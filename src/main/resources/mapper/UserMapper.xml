<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="capstone_project.ReadingForum_Backend.Dao.UserDao">
    <select id="selectAll" resultType="capstone_project.ReadingForum_Backend.Model.User">
        select * from user
    </select>

    <select id="selectById" resultType="capstone_project.ReadingForum_Backend.Model.User">
        select * from user where id=#{id}
    </select>

    <select id="selectByUsername" resultType="capstone_project.ReadingForum_Backend.Model.User">
        select * from user where username=#{username}
    </select>

    <insert id="insert">
        insert into user (username, password) values (#{username}, #{password})
    </insert>

    <update id="updateById">
        update user set password=#{password}, nickname=#{nickname}, avatar=#{avatar}, gender=#{gender}, birthday=#{birthday}, phone=#{phone}, email=#{email}, location=#{location}, bio=#{bio}, state=#{state}, deleted=#{deleted} where id=#{id}
    </update>

    <update id="updateByUsername">
        update user set password=#{password}, nickname=#{nickname}, avatar=#{avatar}, gender=#{gender}, birthday=#{birthday}, phone=#{phone}, email=#{email}, location=#{location}, bio=#{bio}, state=#{state}, deleted=#{deleted} where username=#{username}
    </update>
</mapper>